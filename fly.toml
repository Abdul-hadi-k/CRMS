app = "crms"
primary_region = "sin"

[build]
builder = "paketobuildpacks/builder:base"

[env]
PORT = "8080"

[http_service]
internal_port = 8080
force_https = true

[[vm]]
memory = "512mb"
cpu_kind = "shared"
cpus = 1